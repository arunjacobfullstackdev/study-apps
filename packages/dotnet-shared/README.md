# dotnet-shared

Reusable .NET class library for shared logic across .NET apps in this monorepo.

## Exports

- `GreetingBuilder.BuildGreeting(name)`: returns a normalized greeting string.

## Scripts

- `pnpm --filter dotnet-shared build`
- `pnpm --filter dotnet-shared test`
